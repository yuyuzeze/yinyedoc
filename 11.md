h2. 背景

アプリ方式設計書（第7章 DBアクセス方式）において、楽観的排他制御の実装方式として rowversion を採用した。
しかし現在のテーブル設計書（OKI-Q-824_TBL-0124）には RowVersion 列の定義が存在しないため、確認が必要。

---

h2. 調査証跡

楽観的排他制御の実装方式として以下の3案を調査した。

h3. 方式① datetime（既存 dt_Update カラムをそのまま使用）

dt_Update は現在ほぼ全テーブルに存在しているため、DDL変更なしで排他制御に使用できる。

ただし datetime 型は精度が約3.33ms しかなく、アプリがセットした値がDBに保存される際に四捨五入されてしまう。

*例：*
* アプリがセットした値　：10:00:00.001
* DBに実際に保存される値：10:00:00.000（四捨五入）

EF Core は「自分がセットした値」と「DBに実際に保存された値」を比較して一致しない場合に「他のユーザーが更新した」と判断する。
上記の例では誰も更新していないのに値が変わってしまうため、約2/3の確率で誤ったエラーが発生する。

*推奨度：△（解決策はあるが複雑）*

h3. 方式② datetime2（dt_Update の型を変更）

datetime を datetime2 に型変更することで精度が100ナノ秒に向上し、四捨五入が発生しなくなる。
アプリ側で dt_Update を更新する処理は引き続き必要だが、誤エラーの問題は解消される。

DDL変更（ALTER TABLE による型変更）が必要。既存データは自動変換されるためデータ移行作業は不要。

*推奨度：✅ 推奨*

h3. 方式③ rowversion（新規列を追加）

SQL Server が提供する rowversion 型の列を追加する。
INSERT・UPDATE のたびに SQL Server が自動的に値を更新するため、アプリ側でのセット処理が一切不要となる。
四捨五入も発生しないため誤エラーもない。

DDL変更（ALTER TABLE による列追加）が必要。既存データには自動的に値が採番される。

*動作確認：*
TM_SYUSHIM_M（185万件）に対して列追加を実行したところ53秒で完了し、既存全行に RowVersion が自動採番されることを確認済み。

*推奨度：✅ 推奨（最も）*

h3. 方式比較

|_.項目|_.方式① datetime|_.方式② datetime2|_.方式③ rowversion|
|現状の既存カラム|dt_Update（既存テーブルに存在）|なし（型変更が必要）|なし（列追加が必要）|
|EF Core 属性|[ConcurrencyCheck]|[ConcurrencyCheck]|[Timestamp]|
|DDL変更|不要|ALTER TABLE（型変更）|ALTER TABLE（列追加）|
|SaveChanges内セット処理|必要（手動）|必要（手動）|不要（SQL Server自動管理）|
|誤エラー発生リスク|あり|なし|なし|
|推奨度|△|✅ 推奨|✅ 推奨（最も）|

h3. 既存テーブル調査結果

|_.項目|_.結果|
|調査対象テーブル数|183件（TBL-0001〜TBL-0183、TBL-0026不要）|
|dt_Update（datetime型）あり|約153件|
|更新日時項目なし（CUD操作なし）|20件|
|rowversion カラムあり|0件|
|datetime2 型カラムあり|0件|

---

h2. 対応方針

*採用方式：方式③ rowversion*

*理由：*
* 誤エラーが発生しない
* アプリ側での値のセット処理が不要（SQL Server が完全自動管理）
* INSERT / UPDATE 時に SQL Server が自動で値を更新する

*対応内容：*

# 排他制御が必要な全テーブル（約163件）に RowVersion 列を追加
** CUD操作なし20件は対応不要
** dt_Update は変更しない
# テーブル定義書の修正
** 対象：OKI-Q-824_TBL-0124-テーブル定義書
** 追加内容：論理名「rowversion」／物理名「RowVersion」／データ型「timestamp」／Not Null「Yes」

---

h2. 確認内容

上記の対応方針（方式③ rowversion の採用）で進めてよいかご確認をお願いします。
