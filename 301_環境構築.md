# 301. 環境構築

**林業信用基金基幹システム 再構築プロジェクト**

| 項目 | 内容 |
|------|------|
| 文書番号 | 301 |
| 文書名 | 環境構築手順書 |
| 版数 | 1.00 |
| 作成日 | 2026/08/06 |
| 作成者 | OKI |
| 対象読者 | 開発担当者・テスト担当者・基盤担当者 |

---

## 1. 本書の目的と適用範囲

### 1.1 目的

本書は、林業信用基金基幹システム（Angular 20 + .NET 10）の開発・テストに必要な各環境の
構築手順、必要なインストールパッケージ、コード／DB のデプロイ手順を規定する。

### 1.2 適用範囲

| 区分 | 環境名 | 用途 | 本書での章 |
|------|--------|------|-----------|
| ① | **開発ローカル環境** | 開発者 PC（Windows 11）。Visual Studio から直接起動して開発・デバッグ | 第4章 |
| ② | **テストローカル環境** | ローカル Windows Server（IIS）。結合前の擬似本番構成での動作確認 | 第5章 |
| ③ | **テスト Azure 環境** | 個人／検証用 Azure サブスクリプション上に本番同等構成を再現 | 第6章 |
| ④ | 本番環境（JAFFIC クラウド） | JAFFIC クラウド事業者が構築。**本書の対象外**（別途 移行計画書にて規定） | — |

> **注記**: ④ 本番環境は JAFFIC クラウド事業者側の管理範囲であり、アプリケーション配置手順のみ
> 第6章のテスト Azure 環境手順に準じる。ネットワーク・FW 申請は別途調整とする。

### 1.3 関連文書

| 文書番号 | 文書名 |
|---------|--------|
| — | プロジェクトコンテキスト定義書 v8 |
| — | 方式設計書（アーキテクチャ） |
| — | 外部インターフェース設計書（IF-X0010 格付判定サービス） |

---

## 2. 環境全体像

### 2.1 環境構成比較

| 項目 | ① 開発ローカル | ② テストローカル | ③ テスト Azure |
|------|--------------|----------------|---------------|
| フロントエンド実行 | `ng serve`（localhost:4200） | IIS 静的配信（Root `/`） | IIS 静的配信（Root `/`） |
| バックエンド実行 | Kestrel（localhost:5001） | IIS Sub-App `/api` | IIS Sub-App `/api` |
| DB | SQL Server Express / LocalDB | SQL Server Express or Developer | Azure SQL Database |
| DB 認証 | SQL 認証（ローカルアカウント） | SQL 認証 | Microsoft Entra ID（Managed Identity） |
| 認証（画面） | **OFF**（`auth.enabled=false`）またはテナント SSO | Entra ID SSO | Entra ID SSO |
| ファイルストレージ | ローカルフォルダ or Azurite | ローカルフォルダ | Azure Blob Storage |
| シークレット | User Secrets / appsettings.Development.json | appsettings.Test.json | Azure Key Vault |
| ログ | コンソール + ファイル | ファイル | ファイル + AMA → Log Analytics |
| メール送信 | 送信スタブ（送信しない） | 送信スタブ | SMTP Relay（要 FW 許可） |
| 格付判定 API | モックスタブ | モックスタブ | FEG 検証環境 or モック |

### 2.2 デプロイ構成（②③ 共通・本番同等）

```
Windows Server 2022
└── IIS 10
    └── Web サイト:  https://<host>:443
        ├── /     (Root)     → Angular 20 ビルド成果物（静的ファイル）
        │                       アプリプール: Pool_Frontend（マネージドコードなし）
        │                       URL Rewrite で SPA フォールバック
        └── /api  (Sub-App)  → .NET 10 Web API
                                アプリプール: Pool_Api（マネージドコードなし）
                                AspNetCoreModuleV2 → Kestrel（in-process）
```

* Root と `/api` が同一オリジンのため **CORS 設定は不要**（`apiUrl = '/api'`）。
* 開発ローカルのみポートが分かれるため、`proxy.conf.json` で `/api` を 5001 に転送する。

---

## 3. 必要インストールパッケージ一覧

### 3.1 開発ローカル PC（必須）

| No. | ソフトウェア | 推奨バージョン | 入手先 | 備考 |
|-----|------------|--------------|--------|------|
| 1 | **Visual Studio 2026**（Community/Professional/Enterprise） | 18.x 最新 | https://visualstudio.microsoft.com/ | **★.NET 10 を対象とする開発は VS 2026 必須。VS 2022（17.x）は .NET 10 のターゲット設定に非対応** |
| 2 | .NET 10 SDK | 10.0.1xx | https://dotnet.microsoft.com/download/dotnet/10.0 | VS 2026 インストール時に同梱。単体導入も可 |
| 3 | **Node.js** | **22.x LTS** | https://nodejs.org/ | Angular 20 要件: `^20.19.0 \|\| ^22.12.0 \|\| >=24.0.0` |
| 4 | Angular CLI | 20.0.x | `npm install -g @angular/cli@20` | グローバルインストール |
| 5 | Git for Windows | 最新 | https://git-scm.com/ | VS 2026 同梱の Git でも可 |
| 6 | SQL Server Express | 2022 | https://www.microsoft.com/sql-server/sql-server-downloads | ローカル業務 DB。LocalDB でも可 |
| 7 | SQL Server Management Studio (SSMS) | 20.x 以降 | https://aka.ms/ssms | DB 操作・スクリプト実行 |
| 8 | Azure Data Studio または A5:SQL Mk-2 | 最新 | — | ER 図・データ確認用（任意） |

**Visual Studio 2026 のインストール時に選択するワークロード**

| ワークロード | 用途 |
|------------|------|
| ASP.NET と Web 開発 | .NET 10 Web API 開発（必須） |
| Node.js 開発 | Angular プロジェクトを VS 上で起動する場合（推奨） |
| .NET デスクトップ開発 | 旧 VB.NET 資産の参照・ActiveReports 変換ツール利用時（任意） |
| データの保存と処理 | SQL Server プロジェクト・SSDT（任意） |

### 3.2 開発ローカル PC（推奨・任意）

| No. | ソフトウェア | 用途 |
|-----|------------|------|
| 1 | Visual Studio Code | Angular 側の編集。拡張: Angular Language Service / ESLint / Prettier |
| 2 | Azure CLI | Azure リソース操作・ログイン（`az login`） |
| 3 | AzCopy v10 | Blob Storage への手動アップロード検証 |
| 4 | Azurite（`npm i -g azurite`） | Blob Storage のローカルエミュレータ |
| 5 | Postman / REST Client | API 単体テスト（Swagger UI で代替可） |
| 6 | GitHub Copilot（VS 拡張） | `.github/copilot-instructions.md` 連携 |

### 3.3 商用ライブラリ（ライセンス取得・別途手配が必要）

| No. | 製品 | バージョン | 提供元 | 導入方法 | 備考 |
|-----|------|----------|--------|---------|------|
| 1 | **ActiveReports for .NET** | **v20** | MESCIUS（旧グレープシティ） | 開発者ライセンスキーを VS に登録 + NuGet | PDF 帳票出力。旧システム v14.3 からの移行 |
| 2 | **ExcelCreator** | **v12** | アドバンスソフトウェア社 | ベンダー提供 NuGet フィード | Excel テンプレート帳票。旧システム v10 からの移行 |
| 3 | MESCIUS File Converter | ActiveReports v20 同梱 | MESCIUS | ActiveReports インストーラに同梱 | 旧 `.vb` レポートの名前空間自動変換 |

> **★重要**: 商用ライブラリは**開発者ごとにライセンスが必要**。ライセンス未取得の開発者は
> 帳票機能をビルド対象外にできるよう、`Api.csproj` の条件付き参照を利用する。
>
> ```xml
> <PackageReference Include="ExcelCreator.Xlsx.Creator.v12" Version="12.0.0"
>                   Condition="'$(ExcelCreatorEnabled)' == 'true'" />
> ```
>
> ライセンス保有者のみローカルで以下を設定する。
> ```powershell
> # ユーザ環境変数として設定（VS 再起動で反映）
> setx ExcelCreatorEnabled true
> ```

### 3.4 サーバ側（テストローカル / テスト Azure 共通）

| No. | ソフトウェア | バージョン | 備考 |
|-----|------------|----------|------|
| 1 | Windows Server | 2022 | OS |
| 2 | IIS（Web サーバー役割） | 10 | 「Web サーバー(IIS)」役割を追加 |
| 3 | **ASP.NET Core 10.0 Hosting Bundle** | 10.0.x | **必須**。AspNetCoreModuleV2 を IIS に登録 |
| 4 | **URL Rewrite Module 2.1** | 2.1 | SPA フォールバック（Angular ルーティング）に必須 |
| 5 | Application Request Routing (ARR) | 3.0 | 任意。リバースプロキシ構成時のみ |
| 6 | OpenSSH Server | Windows 機能 | 外部システムとの SFTP ファイル交換（本番同等検証時） |
| 7 | AzCopy v10 | 最新 | Blob Storage 連携検証 |
| 8 | ActiveReports v20 ランタイム | v20 | 帳票出力を伴う場合。※配布ライセンス条件を確認のこと |
| 9 | ExcelCreator v12 ランタイム | v12 | 同上 |

**IIS 役割サービスの選択項目**

```
Web サーバー (IIS)
├── Web サーバー
│   ├── 共通 HTTP 機能  … 既定のドキュメント / 静的なコンテンツ / HTTP エラー
│   ├── 正常性と診断      … HTTP ログ / 要求の監視
│   ├── パフォーマンス    … 静的なコンテンツの圧縮 / 動的なコンテンツの圧縮
│   └── セキュリティ      … 要求フィルタリング / (必要に応じ) Windows 認証
└── 管理ツール           … IIS 管理コンソール
```

### 3.5 NuGet パッケージ（バックエンド）

`src/Api/Api.csproj` ほか各プロジェクトの `PackageReference` により自動復元される。
主なパッケージは以下のとおり。

| パッケージ | バージョン | 用途 |
|-----------|----------|------|
| Microsoft.EntityFrameworkCore（+ .SqlServer / .Design） | 10.0.x | ORM・マイグレーション |
| Microsoft.Identity.Web | 3.2.0 | Entra ID JWT 検証 |
| Microsoft.AspNetCore.Authentication.JwtBearer | 10.0.0 | JWT 認証 |
| Swashbuckle.AspNetCore | 7.2.0 | Swagger / OpenAPI |
| AutoMapper | 13.0.1 | Entity ↔ DTO |
| FluentValidation（+ DependencyInjectionExtensions） | 11.11.0 | 入力・業務バリデーション |
| Serilog.AspNetCore / .Sinks.File / .Sinks.Console / .Settings.Configuration | 10.0.x | ログ出力 |
| Azure.Identity | 1.14.2 | Managed Identity 認証 |
| Azure.Storage.Blobs | 12.25.1 | Blob Storage 操作 |
| Azure.Security.KeyVault.Secrets | 4.8.0 | Key Vault |
| Polly | 8.5.0 | 外部 API リトライ／サーキットブレーカー |
| CsvHelper | 31.x | OCR CSV パース |
| MailKit | 最新 | SMTP メール送信 |
| ClosedXML | 0.102.x | Excel アドホック出力（MIT） |
| Dapper | 2.1.x | 複雑クエリ |
| ExcelCreator.Xlsx.Creator.v12 | 12.0.0 | Excel テンプレート帳票（**条件付き**） |

**`nuget.config` の設定（ExcelCreator 利用時のみ）**

リポジトリ直下の `nuget.config.example` を `nuget.config` にコピーし、
ベンダー提供フィード URL を設定する。`nuget.config` は `.gitignore` 対象とする。

```xml
<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <packageSources>
    <clear />
    <add key="nuget.org" value="https://api.nuget.org/v3/index.json" />
    <add key="excelcreator" value="https://<ベンダー提供フィードURL>/v3/index.json" />
  </packageSources>
</configuration>
```

### 3.6 npm パッケージ（フロントエンド）

`package.json` により `npm ci` で自動導入される。主なパッケージは以下。

| パッケージ | バージョン | 用途 |
|-----------|----------|------|
| @angular/* | 20.0.x | Angular コア一式 |
| @angular/material, @angular/cdk | 20.0.x | UI コンポーネント（Material 3） |
| @azure/msal-angular / @azure/msal-browser | 3.x | Entra ID SSO |
| rxjs | 7.8.x | リアクティブ処理 |
| typescript | 5.8.x | コンパイラ（Angular 20 要件: `>=5.8 <5.9`） |
| ngx-toastr | 19.x | トースト通知 |
| date-fns | 3.x | 日付処理 |
| file-saver | 2.0.x | ファイルダウンロード |

---

## 4. ① 開発ローカル環境の構築

### 4.1 構築手順サマリ

| No. | 作業 | 所要 |
|-----|------|------|
| 1 | 前提ソフトのインストール（3.1 節） | 1〜2 時間 |
| 2 | リポジトリのクローン | 10 分 |
| 3 | ローカル DB 構築 | 30 分 |
| 4 | バックエンド設定・復元 | 20 分 |
| 5 | フロントエンド設定・復元 | 20 分 |
| 6 | Visual Studio 一括起動設定 | 20 分 |
| 7 | 動作確認 | 20 分 |

### 4.2 リポジトリ取得とローカルフォルダ構成

**推奨ローカル配置**（Visual Studio 一括起動の相対パス前提）

```
D:\Work\linye\
├── linyebackend\            ← バックエンドリポジトリ
│   ├── Nogyosuisan.sln
│   ├── nuget.config          （ExcelCreator 利用時のみ・Git 管理外）
│   └── src\
│       ├── Api\
│       ├── Application\
│       ├── Domain\
│       └── Infrastructure\
├── linyefrontend\           ← フロントエンドリポジトリ
│   ├── angular.json
│   ├── package.json
│   ├── proxy.conf.json
│   └── src\
└── db\                       ← DB 構築スクリプト（任意配置）
```

```powershell
cd D:\Work\linye
git clone <backend-repo-url> linyebackend
git clone <frontend-repo-url> linyefrontend
```

> **★注意**: フロントエンドとバックエンドは**別リポジトリ**である。
> Visual Studio から一括起動する場合、上記の相対配置を前提とするため
> **フォルダ名・階層を変更しないこと**。

### 4.3 ローカル DB の構築

#### 4.3.1 SQL Server Express のインストールと初期設定

1. SQL Server 2022 Express をインストール（既定インスタンス名: `SQLEXPRESS`）
2. SQL Server 構成マネージャーで以下を有効化
   - TCP/IP プロトコルを **有効**
   - SQL Server Browser サービスを **自動起動**
3. SSMS で接続し、**混合モード認証（SQL Server 認証と Windows 認証）** を有効化
4. アプリ用ログインを作成

```sql
-- ログイン作成
CREATE LOGIN linye_app WITH PASSWORD = '<開発用パスワード>', CHECK_POLICY = OFF;
GO

-- 業務 DB 作成
CREATE DATABASE GovSystemDB
  COLLATE Japanese_CI_AS;
GO

USE GovSystemDB;
GO
CREATE USER linye_app FOR LOGIN linye_app;
ALTER ROLE db_owner ADD MEMBER linye_app;   -- 開発ローカルのみ。テスト以降は最小権限
GO
```

> **照合順序**: 日本語データを扱うため `Japanese_CI_AS` を指定する。
> Azure SQL Database 側も同一照合順序で作成すること（第6章）。

#### 4.3.2 スキーマ適用（EF Core マイグレーション）

```powershell
# EF Core ツールの導入（初回のみ）
dotnet tool install --global dotnet-ef

cd D:\Work\linye\linyebackend\src\Api

# マイグレーション適用（DB 作成 + テーブル作成）
dotnet ef database update --project ..\Infrastructure --startup-project .
```

**マイグレーションを新規追加する場合**

```powershell
dotnet ef migrations add <MigrationName> --project ..\Infrastructure --startup-project .
dotnet ef database update --project ..\Infrastructure --startup-project .
```

> Visual Studio のパッケージマネージャーコンソールから実行する場合は
> `Add-Migration <Name>` / `Update-Database`（既定プロジェクト = `Infrastructure`）。

#### 4.3.3 初期データ投入

| 種別 | 投入方法 | 備考 |
|------|---------|------|
| マスタ初期データ（コード類） | `db/seed/01_master.sql` を SSMS で実行 | コードマスタ・区分値 |
| UserRole（ロール定義） | `db/seed/02_userrole.sql` | 開発時は自身の UPN / OID を登録 |
| 業務検証データ | 旧システムからの抽出データ（匿名化済み） | 別途 データ移行設計書 参照 |

**UserRole 登録例（開発ローカル・認証 OFF 時のダミー）**

```sql
INSERT INTO UserRole (user_oid, upn, role_cd, valid_flg, updated_at, updated_by)
VALUES ('00000000-0000-0000-0000-000000000001', 'dev.local@example.jp',
        'ADMIN', 1, SYSDATETIME(), 'INIT');
```

### 4.4 バックエンドの設定

#### 4.4.1 `appsettings.Development.json`

`src/Api/appsettings.Development.json` を作成（Git 管理対象外とする）。

```json
{
  "ConnectionStrings": {
    "DefaultConnection": "Server=localhost\\SQLEXPRESS;Database=GovSystemDB;User Id=linye_app;Password=<開発用パスワード>;TrustServerCertificate=True;MultipleActiveResultSets=True"
  },
  "AzureAd": {
    "Instance": "https://login.microsoftonline.com/",
    "TenantId": "<TENANT_ID>",
    "ClientId": "<API_APP_CLIENT_ID>",
    "Audience": "api://<API_APP_CLIENT_ID>"
  },
  "Auth": {
    "Enabled": false
  },
  "Storage": {
    "Mode": "LocalFolder",
    "LocalRoot": "D:\\Work\\linye\\_localstorage",
    "Containers": {
      "Uploads": "uploads",
      "CsvImport": "csv-import",
      "CsvProcessed": "csv-processed"
    }
  },
  "Mail": {
    "Mode": "Stub"
  },
  "ExternalApi": {
    "Kakuzuke": {
      "BaseUrl": "http://localhost:5099/mock/kakuzuke",
      "Mode": "Mock"
    }
  },
  "Serilog": {
    "MinimumLevel": { "Default": "Debug" },
    "WriteTo": [
      { "Name": "Console" },
      {
        "Name": "File",
        "Args": {
          "path": "logs/govlog-.txt",
          "rollingInterval": "Day",
          "retainedFileCountLimit": 30
        }
      }
    ]
  }
}
```

> **★セキュリティ**: 接続文字列・シークレットは `appsettings.Development.json` に直接書かず、
> **User Secrets** の利用を推奨する。
>
> ```powershell
> cd src\Api
> dotnet user-secrets init
> dotnet user-secrets set "ConnectionStrings:DefaultConnection" "Server=localhost\SQLEXPRESS;..."
> ```

#### 4.4.2 起動プロファイル `launchSettings.json`

`src/Api/Properties/launchSettings.json`

```json
{
  "profiles": {
    "Api": {
      "commandName": "Project",
      "launchBrowser": false,
      "applicationUrl": "http://localhost:5001",
      "environmentVariables": {
        "ASPNETCORE_ENVIRONMENT": "Development"
      }
    },
    "Api (SpaProxy)": {
      "commandName": "Project",
      "launchBrowser": true,
      "launchUrl": "http://localhost:4200",
      "applicationUrl": "http://localhost:5001",
      "environmentVariables": {
        "ASPNETCORE_ENVIRONMENT": "Development",
        "ASPNETCORE_HOSTINGSTARTUPASSEMBLIES": "Microsoft.AspNetCore.SpaProxy"
      }
    }
  }
}
```

#### 4.4.3 パッケージ復元とビルド確認

```powershell
cd D:\Work\linye\linyebackend
dotnet restore
dotnet build -c Debug
```

> ExcelCreator ライセンス未取得の場合、`ExcelCreatorEnabled` 環境変数を設定しないことで
> 当該パッケージ参照がスキップされ、ビルドが通る。

### 4.5 フロントエンドの設定

```powershell
cd D:\Work\linye\linyefrontend
npm ci          # package-lock.json に厳密準拠（推奨）
```

**`proxy.conf.json`**（リポジトリ同梱）

```json
{
  "/api": {
    "target": "http://localhost:5001",
    "secure": false,
    "changeOrigin": true
  }
}
```

**`src/environments/environment.development.ts`**（開発時・認証 OFF）

```typescript
export const environment: AppEnvironment = {
  production: false,
  apiUrl: '/api',
  auth: {
    enabled: false,          // ← MsalGuard をスキップ
    useSilentSso: false,
    clientId: '',
    tenantId: '',
    authority: '',
    redirectUri: 'http://localhost:4200',
    postLogoutRedirectUri: 'http://localhost:4200',
    apiScopes: []
  },
  logging: {
    consoleEnabled: true,
    reportToApi: true,
    reportMinLevel: 'error',
    clientLogPath: '/client-log'
  }
};
```

> **認証 OFF 開発モード**: `auth.enabled = false` の場合、`MsalGuard` を通らず
> `AuthContextService.ensureLoaded()` が `/api/auth/me` を呼び出してユーザ情報を取得する。
> バックエンド側も `Auth:Enabled = false` に合わせること。

**単体起動での確認**

```powershell
npm start        # = ng serve --proxy-config proxy.conf.json
# → http://localhost:4200
```

### 4.6 ★ Visual Studio からの一括起動

開発ローカルでは **Visual Studio の F5 一発でフロント・バックエンドの両方を起動**できる構成とする。
以下の 3 方式のうち、**方式 A（SPA Proxy）を標準とする**。

#### 方式 A: SPA Proxy（推奨・標準）

`Microsoft.AspNetCore.SpaProxy` を使用し、API 起動時に `ng serve` を自動起動する。

**手順 1: `Api.csproj` に以下を追記**

```xml
<PropertyGroup>
  <!-- フロントエンドリポジトリへの相対パス -->
  <SpaRoot>..\..\..\linyefrontend\</SpaRoot>
  <SpaProxyServerUrl>http://localhost:4200</SpaProxyServerUrl>
  <SpaProxyLaunchCommand>npm start</SpaProxyLaunchCommand>
</PropertyGroup>

<ItemGroup>
  <PackageReference Include="Microsoft.AspNetCore.SpaProxy" Version="10.0.0"
                    Condition="'$(Configuration)' == 'Debug'" />
</ItemGroup>
```

**手順 2: Visual Studio で起動**

1. `Nogyosuisan.sln` を Visual Studio 2026 で開く
2. スタートアッププロジェクトを `Api` に設定
3. 起動プロファイルで **`Api (SpaProxy)`** を選択
4. **F5**（デバッグ実行）

**動作**

```
F5
 └→ Kestrel 起動（http://localhost:5001）
 └→ SpaProxy が linyefrontend で `npm start` を自動実行
 └→ ng serve が 4200 で待ち受け
 └→ ブラウザが http://localhost:4200 を自動オープン
 └→ 画面からの /api リクエストは proxy.conf.json 経由で 5001 に転送
 └→ C# 側のブレークポイントがそのまま有効
```

> **メリット**: ターミナルを別に開く必要がない。C# のブレークポイントが即使える。
> **注意点**:
> - 初回は `ng serve` のコンパイルに 30〜60 秒かかるため、SpaProxy のタイムアウト前に
>   起動しない場合がある。その場合は先に `npm start` を手動で起動しておけば、
>   SpaProxy は既存プロセスを検出して再利用する。
> - VS 終了時に `node` プロセスが残ることがある。残った場合は
>   `taskkill /F /IM node.exe` で終了させる。

#### 方式 B: 複数スタートアッププロジェクト（.esproj）

Angular プロジェクトを JavaScript プロジェクト（`.esproj`）としてソリューションに追加し、
「複数のスタートアッププロジェクト」で同時起動する。

1. Visual Studio で `Nogyosuisan.sln` を開く
2. ソリューション右クリック → 追加 → 既存の Web サイト／プロジェクト
   → 「既存の Angular アプリから作成」を選択し `..\..\linyefrontend` を指定
   （生成される `linyefrontend.esproj` は **Git 管理対象外**にする）
3. ソリューション右クリック → プロパティ → スタートアッププロジェクト
   → 「複数のスタートアッププロジェクト」を選択

   | プロジェクト | アクション |
   |------------|-----------|
   | `Api` | 開始 |
   | `linyefrontend` | 開始（デバッグなし） |

4. F5

> **メリット**: フロント／バックの起動順・可否を個別制御できる。
> **注意点**: 別リポジトリのプロジェクトを取り込むため、`.sln` に相対パスが記録される。
> 各開発者のフォルダ配置が 4.2 節と異なると壊れるため、`.sln` の変更をコミットしないこと。

#### 方式 C: 手動起動（フォールバック）

方式 A/B が不調な場合の確実な手段。

| 端末 | コマンド |
|------|---------|
| ターミナル 1 | `cd linyebackend\src\Api` → `dotnet run` |
| ターミナル 2 | `cd linyefrontend` → `npm start` |

Visual Studio では `Api` のみ F5 で起動し、フロントは別ターミナルで `npm start` する運用でも良い。

### 4.7 動作確認チェックリスト

| No. | 確認項目 | 期待結果 |
|-----|---------|---------|
| 1 | `http://localhost:5001/swagger` | Swagger UI が表示される |
| 2 | Swagger から `GET /api/auth/me` 実行 | 200 応答。認証 OFF 時はダミーユーザが返る |
| 3 | `http://localhost:4200` | ログイン後トップ画面（または認証 OFF でそのまま画面）が表示 |
| 4 | 画面から一覧検索を実行 | DevTools の Network で `/api/...` が 200、データが表示される |
| 5 | C# の Controller にブレークポイント | F5 実行中に停止する |
| 6 | `src/Api/logs/govlog-yyyyMMdd.txt` | リクエストログが出力されている |
| 7 | SSMS で `GovSystemDB` のテーブル一覧 | マイグレーション済みテーブルが存在する |

### 4.8 開発時の外部依存のスタブ化

本番でのみ利用可能な外部依存は、開発ローカルではスタブに切り替える。

| 依存 | 開発ローカルでの扱い | 設定キー |
|------|-------------------|---------|
| Entra ID 認証 | OFF（ダミーユーザ） | `Auth:Enabled = false` / `environment.auth.enabled = false` |
| Azure Blob Storage | ローカルフォルダ or Azurite | `Storage:Mode = LocalFolder` / `Azurite` |
| Azure Key Vault | User Secrets | `KeyVault:Enabled = false` |
| SMTP メール送信 | 送信せずログ出力のみ | `Mail:Mode = Stub` |
| 格付判定サービス（FEG） | モック応答 | `ExternalApi:Kakuzuke:Mode = Mock` |
| 財務会計（BASE-One）連携 | ローカルフォルダ入出力 | `Storage:Mode = LocalFolder` |

**Azurite を使う場合**

```powershell
npm install -g azurite
azurite --silent --location D:\Work\linye\_azurite
# 接続文字列: UseDevelopmentStorage=true
```

---

## 5. ② テストローカル環境（Windows Server + IIS）の構築

### 5.1 目的

本番と同一の **VM + IIS（Root `/` + Sub-App `/api`）** 構成を、ローカル Windows Server 上に
再現し、発行成果物ベースでの動作確認を行う。

### 5.2 サーバ側 前提ソフトの導入

| 順序 | 作業 |
|------|------|
| 1 | サーバーマネージャー → 役割の追加 → **Web サーバー (IIS)**（3.4 節の役割サービスを選択） |
| 2 | **ASP.NET Core 10.0 Hosting Bundle** をインストール → `iisreset` |
| 3 | **URL Rewrite Module 2.1** をインストール |
| 4 | （必要に応じ）SQL Server 2022 Express / Developer をインストール |
| 5 | （必要に応じ）ActiveReports v20 / ExcelCreator v12 のランタイムを導入 |

**Hosting Bundle 導入確認**

```powershell
# AspNetCoreModuleV2 が登録されていること
Get-WebGlobalModule | Where-Object { $_.Name -like "AspNetCoreModule*" }

# インストール済みランタイム
dotnet --list-runtimes
# → Microsoft.AspNetCore.App 10.0.x が存在すること
```

### 5.3 成果物のビルド・発行

開発 PC 上でビルドし、成果物をサーバへ配布する。

```powershell
# ---------- ① フロントエンド ----------
cd D:\Work\linye\linyefrontend
npm ci
ng build --configuration production
#   出力: dist\<project-name>\browser\

# ---------- ② バックエンド ----------
cd D:\Work\linye\linyebackend\src\Api
dotnet publish -c Release -o D:\Work\linye\_publish\api
#   出力: _publish\api\  （Api.dll, web.config, 依存 DLL 一式）
```

**配布パッケージ構成**

```
_publish\
├── web\          ← linyefrontend\dist\<project>\browser\ の中身をコピー
│   ├── index.html
│   ├── main-*.js
│   ├── styles-*.css
│   └── web.config          （SPA フォールバック用・5.5 節）
└── api\          ← dotnet publish の出力
    ├── Api.dll
    ├── appsettings.json
    ├── appsettings.Test.json
    └── web.config          （dotnet publish が自動生成）
```

### 5.4 IIS サイト・アプリケーションプールの構成

#### 5.4.1 フォルダ配置

| 論理 | 物理パス |
|------|---------|
| フロントエンド（Root） | `C:\inetpub\linye\web\` |
| バックエンド（Sub-App） | `C:\inetpub\linye\api\` |
| ログ出力 | `C:\inetpub\linye\api\logs\` |

#### 5.4.2 アプリケーションプール

| プール名 | .NET CLR バージョン | パイプライン | ID | 用途 |
|---------|-------------------|------------|-----|------|
| `Pool_Frontend` | **マネージドコードなし** | 統合 | ApplicationPoolIdentity | Angular 静的配信 |
| `Pool_Api` | **マネージドコードなし** | 統合 | ApplicationPoolIdentity | .NET 10 Web API |

> ASP.NET Core は Kestrel が自ら実行環境を持つため、必ず「**マネージドコードなし**」を選択する。

#### 5.4.3 サイト・アプリケーションの作成

```powershell
Import-Module WebAdministration

# アプリケーションプール
New-WebAppPool -Name "Pool_Frontend"
Set-ItemProperty IIS:\AppPools\Pool_Frontend -Name managedRuntimeVersion -Value ""
New-WebAppPool -Name "Pool_Api"
Set-ItemProperty IIS:\AppPools\Pool_Api      -Name managedRuntimeVersion -Value ""

# Web サイト（Root = フロントエンド）
New-Website -Name "Linye" -Port 443 -PhysicalPath "C:\inetpub\linye\web" `
            -ApplicationPool "Pool_Frontend" -Ssl

# 子アプリケーション（/api）
New-WebApplication -Site "Linye" -Name "api" `
                   -PhysicalPath "C:\inetpub\linye\api" -ApplicationPool "Pool_Api"
```

#### 5.4.4 アクセス権限

| 対象 | 付与するアカウント | 権限 |
|------|-----------------|------|
| `C:\inetpub\linye\web` | `IIS AppPool\Pool_Frontend` | 読み取りと実行 |
| `C:\inetpub\linye\api` | `IIS AppPool\Pool_Api` | 読み取りと実行 |
| `C:\inetpub\linye\api\logs` | `IIS AppPool\Pool_Api` | **変更（書き込み可）** |

```powershell
icacls "C:\inetpub\linye\api\logs" /grant "IIS AppPool\Pool_Api:(OI)(CI)M" /T
```

### 5.5 web.config

#### 5.5.1 フロントエンド（`C:\inetpub\linye\web\web.config`）

Angular のクライアントサイドルーティングを成立させるため、
**`/api` 以外の未存在パスを `index.html` にフォールバック**させる。

```xml
<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <system.webServer>
    <rewrite>
      <rules>
        <rule name="Angular SPA Fallback" stopProcessing="true">
          <match url=".*" />
          <conditions logicalGrouping="MatchAll">
            <add input="{REQUEST_FILENAME}" matchType="IsFile"      negate="true" />
            <add input="{REQUEST_FILENAME}" matchType="IsDirectory" negate="true" />
            <add input="{REQUEST_URI}" pattern="^/api(/|$)" negate="true" />
          </conditions>
          <action type="Rewrite" url="/index.html" />
        </rule>
      </rules>
    </rewrite>
    <staticContent>
      <remove fileExtension=".json" />
      <mimeMap fileExtension=".json" mimeType="application/json" />
      <remove fileExtension=".woff2" />
      <mimeMap fileExtension=".woff2" mimeType="font/woff2" />
    </staticContent>
    <httpProtocol>
      <customHeaders>
        <add name="X-Content-Type-Options" value="nosniff" />
        <add name="X-Frame-Options" value="SAMEORIGIN" />
      </customHeaders>
    </httpProtocol>
  </system.webServer>
</configuration>
```

> **★重要**: 子アプリ `/api` に親サイトの `web.config` 設定が継承されると
> ASP.NET Core モジュールと競合する場合がある。競合時は
> `<location path="." inheritInChildApplications="false">` で囲むこと。

#### 5.5.2 バックエンド（`C:\inetpub\linye\api\web.config`）

`dotnet publish` により自動生成される。環境変数の指定のみ追記する。

```xml
<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <location path="." inheritInChildApplications="false">
    <system.webServer>
      <handlers>
        <add name="aspNetCore" path="*" verb="*"
             modules="AspNetCoreModuleV2" resourceType="Unspecified" />
      </handlers>
      <aspNetCore processPath="dotnet" arguments=".\Api.dll"
                  stdoutLogEnabled="false" stdoutLogFile=".\logs\stdout"
                  hostingModel="inprocess">
        <environmentVariables>
          <environmentVariable name="ASPNETCORE_ENVIRONMENT" value="Test" />
        </environmentVariables>
      </aspNetCore>
    </system.webServer>
  </location>
</configuration>
```

> 障害調査時のみ `stdoutLogEnabled="true"` に変更し、`logs` フォルダを作成する。
> 常時 true にするとディスクを圧迫するため、調査後は必ず false に戻すこと。

### 5.6 DB 構築（テストローカル）

1. SQL Server Express / Developer に `GovSystemDB` を作成（照合順序 `Japanese_CI_AS`）
2. スキーマ適用は **SQL スクリプト方式**を推奨（サーバに SDK を入れないため）

```powershell
# 開発 PC 側で冪等な SQL スクリプトを生成
cd D:\Work\linye\linyebackend\src\Api
dotnet ef migrations script --idempotent `
    --project ..\Infrastructure --startup-project . `
    --output D:\Work\linye\_publish\db\schema.sql
```

3. 生成した `schema.sql` をサーバに配布し、SSMS または `sqlcmd` で実行

```powershell
sqlcmd -S localhost\SQLEXPRESS -d GovSystemDB -i D:\deploy\db\schema.sql
sqlcmd -S localhost\SQLEXPRESS -d GovSystemDB -i D:\deploy\db\seed\01_master.sql
```

4. `appsettings.Test.json` に接続文字列を設定（最小権限ユーザを使用）

```json
{
  "ConnectionStrings": {
    "DefaultConnection": "Server=localhost\\SQLEXPRESS;Database=GovSystemDB;User Id=linye_app;Password=<パスワード>;TrustServerCertificate=True"
  }
}
```

> **★推奨**: テスト環境以降のアプリ用 DB ユーザは `db_owner` ではなく
> `db_datareader` + `db_datawriter` + 必要なストアド実行権限のみを付与する。

### 5.7 デプロイ手順（更新時）

```powershell
# 1. アプリ停止
Stop-WebAppPool -Name "Pool_Api"
Stop-WebAppPool -Name "Pool_Frontend"

# 2. バックアップ
$stamp = Get-Date -Format "yyyyMMddHHmmss"
Copy-Item C:\inetpub\linye C:\backup\linye_$stamp -Recurse

# 3. 成果物差し替え
Remove-Item C:\inetpub\linye\web\* -Recurse -Force -Exclude web.config
Copy-Item D:\deploy\web\*  C:\inetpub\linye\web\ -Recurse -Force
Copy-Item D:\deploy\api\*  C:\inetpub\linye\api\ -Recurse -Force `
          -Exclude appsettings.Test.json

# 4. DB スキーマ更新（差分がある場合）
sqlcmd -S localhost\SQLEXPRESS -d GovSystemDB -i D:\deploy\db\schema.sql

# 5. アプリ起動
Start-WebAppPool -Name "Pool_Api"
Start-WebAppPool -Name "Pool_Frontend"
```

> **★注意**: `appsettings.Test.json` は環境固有の設定を含むため、
> デプロイ時に**上書きしないこと**。

### 5.8 動作確認チェックリスト

| No. | 確認項目 | 期待結果 |
|-----|---------|---------|
| 1 | `https://<host>/` | Angular のトップ画面が表示される |
| 2 | ブラウザで画面遷移後に **F5 リロード** | 404 にならず同じ画面が再表示（SPA フォールバック確認） |
| 3 | `https://<host>/api/health`（または `/api/auth/me`） | 200 応答 |
| 4 | 一覧検索・登録・更新 | DB に反映される |
| 5 | 帳票出力（PDF / Excel） | ファイルがダウンロードされる |
| 6 | `C:\inetpub\linye\api\logs\govlog-yyyyMMdd.txt` | ログが出力されている |
| 7 | アプリプール再起動後の初回アクセス | 数秒で応答（起動失敗時は Event Viewer を確認） |

---

## 6. ③ テスト Azure 環境の構築

### 6.1 リソース一覧

| 種別 | リソース | SKU / 構成 | 用途 |
|------|---------|----------|------|
| ネットワーク | Virtual Network | 10.0.0.0/16 | 全体 |
| 〃 | Subnet（Web / DB / Storage / Management） | — | 用途別分離 |
| 〃 | NSG | サブネット単位 | 通信制御 |
| 〃 | Azure Bastion | Basic | VM への安全な接続 |
| コンピュート | Azure VM | **B2ms / Windows Server 2022** | IIS + .NET 10 |
| DB | **Azure SQL Database** | S1（テスト用途） | 業務 DB（GovSystemDB） |
| ストレージ | Storage Account（Blob） | StorageV2 / LRS | `uploads` / `csv-import` / `csv-processed` / `backups` |
| セキュリティ | Key Vault | Standard | SMTP 認証情報・証明書 |
| ID | Entra ID アプリ登録 ×2 | — | SPA 用 / API 用 |
| ID | User-assigned Managed Identity | — | VM → SQL / Blob / Key Vault 認証 |
| 監視 | Log Analytics Workspace | PerGB2018 | AMA によるログ収集 |

### 6.2 構築順序

```
① リソースグループ作成
② VNet / Subnet / NSG 作成
③ Managed Identity（ユーザー割り当て）作成
④ Azure SQL Database 作成（Entra ID 管理者を設定）
⑤ Storage Account + コンテナ作成
⑥ Key Vault 作成 + シークレット登録
⑦ Entra ID アプリ登録（SPA / API）
⑧ VM 作成 → MI 割り当て → IIS / Hosting Bundle 導入
⑨ RBAC 付与（MI → Blob / Key Vault）
⑩ SQL に MI 用 DB ユーザ作成
⑪ アプリ配置・DB スキーマ適用
⑫ 動作確認
```

### 6.3 Azure VM の構築と初期設定

```powershell
# VM 作成後、Bastion または RDP で接続して以下を実行

# ① IIS 役割の追加
Install-WindowsFeature -Name Web-Server -IncludeManagementTools
Install-WindowsFeature -Name Web-Http-Redirect, Web-Dyn-Compression, Web-Stat-Compression

# ② ASP.NET Core 10.0 Hosting Bundle をダウンロードして実行
#    https://dotnet.microsoft.com/download/dotnet/10.0 → Hosting Bundle
#    実行後
iisreset

# ③ URL Rewrite Module 2.1 をインストール

# ④ AzCopy（Blob 連携検証用）
#    https://aka.ms/downloadazcopy-v10-windows

# ⑤ OpenSSH Server（SFTP 検証時のみ）
Add-WindowsCapability -Online -Name OpenSSH.Server~~~~0.0.1.0
Start-Service sshd
Set-Service -Name sshd -StartupType Automatic
```

**VM への Managed Identity 割り当て**

Azure Portal → 対象 VM → ID → ユーザー割り当て済み → 追加 → 作成済み MI を選択。

> **★確認事項**: JAFFIC 環境では User-assigned Managed Identity の作成・VM 割り当ての
> 可否、および IMDS エンドポイント（169.254.169.254）へのアクセス可否がネットワーク
> ポリシーに依存する。本番適用前に基盤側へ確認すること（★TBD）。

### 6.4 Azure SQL Database の構築

#### 6.4.1 論理サーバ・DB の作成

| 設定項目 | 値 |
|---------|-----|
| サーバ名 | `sql-linye-test` |
| 認証方法 | **Microsoft Entra ID 認証のみ**（推奨）または混合 |
| Entra ID 管理者 | 構築担当者のアカウント／グループ |
| データベース名 | `GovSystemDB` |
| 照合順序 | **`Japanese_CI_AS`** |
| サービスレベル | S1（テスト用途。本番は要サイジング） |
| バックアップ ストレージ冗長性 | ローカル冗長（テスト） |

#### 6.4.2 ネットワーク設定

| 項目 | 設定 |
|------|------|
| パブリックアクセス | 無効（本番同等）。※構築中のみ一時的に開発者 IP を許可 |
| プライベートエンドポイント | DB Subnet に配置（推奨） |
| または VNet サービスエンドポイント | Web Subnet を許可 |
| 最小 TLS バージョン | 1.2 |

#### 6.4.3 Managed Identity 用 DB ユーザの作成

Entra ID 管理者アカウントで `GovSystemDB` に接続し、以下を実行する。

```sql
-- <MI_NAME> は User-assigned Managed Identity のリソース名
CREATE USER [<MI_NAME>] FROM EXTERNAL PROVIDER;
GO

ALTER ROLE db_datareader ADD MEMBER [<MI_NAME>];
ALTER ROLE db_datawriter ADD MEMBER [<MI_NAME>];
GO

-- ストアド／関数の実行権限が必要な場合
GRANT EXECUTE TO [<MI_NAME>];
GO
```

#### 6.4.4 接続文字列（Managed Identity 認証）

`appsettings.Test.json`（VM 上）

```json
{
  "ConnectionStrings": {
    "DefaultConnection": "Server=tcp:sql-linye-test.database.windows.net,1433;Database=GovSystemDB;Authentication=Active Directory Managed Identity;User Id=<MI_CLIENT_ID>;Encrypt=True;TrustServerCertificate=False;Connection Timeout=30"
  }
}
```

> **開発者ローカルから Azure SQL に接続する場合**は
> `Authentication=Active Directory Default` を指定すると、
> Visual Studio / Azure CLI のサインイン情報が使用される。
> この場合、開発者アカウントにも `CREATE USER ... FROM EXTERNAL PROVIDER` で
> DB ユーザを作成しておく必要がある。

#### 6.4.5 スキーマ適用

```powershell
# 開発 PC 側で冪等スクリプトを生成し、Azure SQL に適用
dotnet ef migrations script --idempotent `
    --project ..\Infrastructure --startup-project . `
    --output schema.sql

sqlcmd -S sql-linye-test.database.windows.net -d GovSystemDB `
       -G -U <your-upn> -i schema.sql
#   -G = Entra ID 認証
```

### 6.5 Storage Account / Key Vault の構築

#### 6.5.1 Blob コンテナ

| コンテナ名 | 用途 | ライフサイクル |
|-----------|------|--------------|
| `uploads` | 画面からのファイルアップロード | Hot → 90日後 Cool |
| `csv-import` | AnyForm OCR からの CSV 受信 | 取込後 `csv-processed` へ移動 |
| `csv-processed` | 取込済み CSV の保管 | 90日後 Cool → 1年後 Archive |
| `backups` | バックアップ | 要件に応じ設定 |

#### 6.5.2 RBAC 付与（Managed Identity → Blob）

| 対象 | ロール | スコープ |
|------|-------|---------|
| VM の User-assigned MI | **ストレージ BLOB データ共同作成者** | Storage Account |
| VM の User-assigned MI | **Key Vault シークレット ユーザー** | Key Vault |

```powershell
az role assignment create `
  --assignee-object-id <MI_PRINCIPAL_ID> --assignee-principal-type ServicePrincipal `
  --role "Storage Blob Data Contributor" `
  --scope /subscriptions/<SUB>/resourceGroups/<RG>/providers/Microsoft.Storage/storageAccounts/<SA>
```

#### 6.5.3 Key Vault シークレット

| シークレット名 | 内容 |
|--------------|------|
| `Smtp--Host` | SMTP サーバホスト名 |
| `Smtp--Port` | ポート番号 |
| `Smtp--UserName` | 認証ユーザ |
| `Smtp--Password` | 認証パスワード |

アプリ側は `DefaultAzureCredential`（VM 上では Managed Identity）で取得する。

#### 6.5.4 OCR 機器用 SAS トークン（参考）

オンプレミスの AnyForm OCR 機器は Managed Identity を使用できないため、
`csv-import` コンテナに対する **書き込み専用 SAS トークン** を発行し、AzCopy で使用する。

```powershell
azcopy copy "C:\AnyForm\out\*.csv" `
  "https://<SA>.blob.core.windows.net/csv-import?<SAS_TOKEN>" --recursive=false
```

| SAS 設定項目 | 値 |
|------------|-----|
| 対象 | コンテナ `csv-import` |
| 許可 | Write / Create（Read・Delete は付与しない） |
| 有効期限 | 1年（定期ローテーション） |
| 許可 IP | OCR 機器のグローバル IP |
| プロトコル | HTTPS のみ |

### 6.6 Entra ID アプリ登録

| # | 用途 | 名前（例） | 設定 |
|---|------|----------|------|
| 1 | **API 用** | `linye-api-test` | 「API の公開」→ アプリケーション ID URI `api://<client-id>`、スコープ `access_as_user` を追加 |
| 2 | **SPA 用** | `linye-spa-test` | プラットフォーム「シングルページアプリケーション」、リダイレクト URI に本番/テスト URL を登録。API のスコープに対する委任アクセス許可を付与 |

**SPA 側リダイレクト URI 登録例**

```
https://<test-host>/
http://localhost:4200          （開発ローカルで SSO を検証する場合）
```

**`environment.ts`（テスト環境向けビルド）**

```typescript
export const environment: AppEnvironment = {
  production: true,
  apiUrl: '/api',
  auth: {
    enabled: true,
    useSilentSso: true,
    clientId: '<SPA_APP_CLIENT_ID>',
    tenantId: '<TENANT_ID>',
    authority: 'https://login.microsoftonline.com/<TENANT_ID>',
    redirectUri: 'https://<test-host>/',
    postLogoutRedirectUri: 'https://<test-host>/',
    apiScopes: ['api://<API_APP_CLIENT_ID>/access_as_user']
  },
  logging: {
    consoleEnabled: false,
    reportToApi: true,
    reportMinLevel: 'error',
    clientLogPath: '/client-log'
  }
};
```

**API 側 `appsettings.Test.json`**

```json
{
  "AzureAd": {
    "Instance": "https://login.microsoftonline.com/",
    "TenantId": "<TENANT_ID>",
    "ClientId": "<API_APP_CLIENT_ID>",
    "Audience": "api://<API_APP_CLIENT_ID>"
  },
  "Auth": { "Enabled": true }
}
```

### 6.7 アプリケーションのデプロイ

VM への配布は以下いずれかの方法による。

| 方式 | 手順 | 適用場面 |
|------|------|---------|
| **A. Blob 経由（推奨）** | 開発 PC で ZIP → AzCopy で Blob へ → VM 側で AzCopy ダウンロード → 展開 | 標準。VM に直接ファイル転送する必要がない |
| B. Bastion / RDP コピー | RDP のクリップボード／ドライブリダイレクトで転送 | 小規模・臨時 |
| C. CI/CD（GitHub Actions 等） | セルフホストランナー or Azure DevOps Agent を VM に導入 | 将来的な自動化（★TBD） |

**方式 A の手順**

```powershell
# ---- 開発 PC ----
Compress-Archive -Path D:\Work\linye\_publish\* -DestinationPath D:\Work\linye\release.zip -Force
azcopy copy "D:\Work\linye\release.zip" `
  "https://<SA>.blob.core.windows.net/deploy/release.zip?<SAS>"

# ---- VM ----
azcopy copy "https://<SA>.blob.core.windows.net/deploy/release.zip?<SAS>" "D:\deploy\release.zip"
Expand-Archive D:\deploy\release.zip -DestinationPath D:\deploy\ -Force

# 以降は 5.7 節「デプロイ手順」と同一
```

### 6.8 動作確認チェックリスト（Azure）

| No. | 確認項目 | 期待結果 |
|-----|---------|---------|
| 1 | Bastion 経由で VM にログイン | 接続できる |
| 2 | `https://<test-host>/` | Entra ID ログイン画面 → 認証後に画面表示 |
| 3 | 認証後の `/api/auth/me` | ユーザ情報とロールが返る |
| 4 | 一覧・登録・更新 | Azure SQL に反映される（SSMS で確認） |
| 5 | ファイルアップロード | `uploads` コンテナに Blob が作成される |
| 6 | CSV 取込（AzCopy で `csv-import` に配置） | BackgroundService が検知し DB 投入、`csv-processed` へ移動 |
| 7 | メール送信（有効化時） | 宛先に到達する（FW 許可要） |
| 8 | 帳票出力（PDF / Excel） | ダウンロードできる |
| 9 | Log Analytics での KQL 検索 | VM のログが収集されている |
| 10 | Managed Identity での DB 接続 | 接続文字列にパスワードが無い状態で成功する |

---

## 7. 環境別設定値一覧

### 7.1 バックエンド（appsettings）

| 設定キー | 開発ローカル | テストローカル | テスト Azure |
|---------|------------|--------------|------------|
| `ASPNETCORE_ENVIRONMENT` | `Development` | `Test` | `Test` |
| `ConnectionStrings:DefaultConnection` | SQL 認証（localhost\SQLEXPRESS） | SQL 認証 | `Authentication=Active Directory Managed Identity` |
| `Auth:Enabled` | `false` | `true` | `true` |
| `AzureAd:TenantId` / `ClientId` | 空 or 検証テナント | 検証テナント | 検証テナント |
| `Storage:Mode` | `LocalFolder` / `Azurite` | `LocalFolder` | `Blob` |
| `KeyVault:Enabled` | `false`（User Secrets） | `false` | `true` |
| `Mail:Mode` | `Stub` | `Stub` | `Smtp` |
| `ExternalApi:Kakuzuke:Mode` | `Mock` | `Mock` | `Mock` or `Real` |
| `Serilog:MinimumLevel` | `Debug` | `Information` | `Information` |
| Swagger | 有効 | 有効（要否は運用判断） | 無効を推奨 |

### 7.2 フロントエンド（environment）

| 設定キー | 開発ローカル | テスト / 本番ビルド |
|---------|------------|------------------|
| `production` | `false` | `true` |
| `apiUrl` | `/api`（proxy 経由 → 5001） | `/api`（同一オリジン） |
| `auth.enabled` | `false` | `true` |
| `auth.useSilentSso` | `false` | `true` |
| `logging.consoleEnabled` | `true` | `false` |
| `logging.reportMinLevel` | `error` | `error` |

### 7.3 使用ポート一覧

| 用途 | 開発ローカル | テスト / Azure |
|------|------------|--------------|
| フロントエンド | 4200（ng serve） | 443（IIS / HTTPS） |
| バックエンド API | 5001（Kestrel） | 443（IIS 子アプリ `/api`） |
| SQL Server | 1433 | 1433（Azure SQL） |
| SFTP（OpenSSH） | — | 22（NSG で送信元 IP 限定） |
| Azurite Blob | 10000 | — |

---

## 8. トラブルシューティング

| 症状 | 想定原因 | 対処 |
|------|---------|------|
| VS の対象フレームワークに .NET 10 が出ない | Visual Studio 2022 を使用している | **Visual Studio 2026 を使用する**（VS 2022 は .NET 10 のターゲット非対応） |
| `dotnet restore` で ExcelCreator が見つからない | `nuget.config` 未設定 / ライセンス未取得 | `ExcelCreatorEnabled` 環境変数を設定しない。または `nuget.config` にベンダーフィードを追加 |
| `npm ci` が失敗する | Node.js のバージョン不一致 | Node.js 22 LTS を使用（`node -v` で確認） |
| 社内プロキシで npm / NuGet が失敗 | プロキシ未設定 | `npm config set proxy <url>` / `nuget.config` の `<config>` にプロキシ設定 |
| F5 で `ng serve` が起動しない | SpaProxy のタイムアウト | 先に `npm start` を手動起動しておく。または方式 C（手動起動） |
| VS 終了後も 4200 が使用中 | `node.exe` の残留 | `taskkill /F /IM node.exe` |
| 画面リロードで 404 | URL Rewrite 未導入 / SPA フォールバック未設定 | URL Rewrite Module を導入し 5.5.1 の `web.config` を配置 |
| `/api` で HTTP 500.19 | 親サイトの設定が子アプリに継承 | `<location path="." inheritInChildApplications="false">` で囲む |
| `/api` で HTTP 500.31（ANCM） | Hosting Bundle 未導入 / バージョン不一致 | ASP.NET Core **10.0** Hosting Bundle を導入し `iisreset` |
| ログファイルが出力されない | アプリプールに書き込み権限がない | `logs` フォルダに `IIS AppPool\Pool_Api` の変更権限を付与 |
| Managed Identity で SQL 接続失敗 | DB ユーザ未作成 / MI 未割り当て | 6.4.3 の `CREATE USER ... FROM EXTERNAL PROVIDER` を実行。VM の ID 設定を確認 |
| MSAL ログイン後に `AADSTS50011` | リダイレクト URI 未登録 | Entra ID アプリ登録の SPA リダイレクト URI に該当 URL を追加 |
| API が 401 を返す | `Audience` 不一致 / スコープ未同意 | `appsettings` の `Audience` と `apiScopes` の整合を確認 |
| 帳票出力で例外 | 商用ライブラリのランタイム／ライセンス未導入 | サーバに ActiveReports v20 / ExcelCreator v12 のランタイムを導入 |
| 日本語が文字化けする | 照合順序 / エンコーディング | DB を `Japanese_CI_AS` で作成。CSV は UTF-8(BOM 付) or Shift_JIS を明示 |

---

## 9. 未確定事項（★TBD）

| No. | 項目 | 内容 | 確認先 |
|-----|------|------|-------|
| 1 | 商用ライブラリのライセンス数 | ActiveReports v20 / ExcelCreator v12 の開発者ライセンス必要数、サーバ配布ライセンス条件 | ベンダー / 調達 |
| 2 | User-assigned Managed Identity | JAFFIC 環境での作成・VM 割り当て可否、IMDS エンドポイントへのアクセス可否 | JAFFIC 基盤 |
| 3 | Azure Blob SFTP 機能 | JAFFIC 環境での利用可否 | JAFFIC 基盤 |
| 4 | SMTP サーバ種別 | オンプレミス Exchange / クラウドリレーのいずれか。FW 申請内容が確定しない | 基金様 |
| 5 | CI/CD 方式 | GitHub Actions セルフホストランナー等の導入可否・ネットワーク要件 | JAFFIC 基盤 |
| 6 | テスト環境の Entra ID テナント | 検証用テナントを別途用意するか、JAFFIC 既存テナントを使用するか | 基金様 / JAFFIC 基盤 |
| 7 | 格付判定サービス検証環境 | FEG 側検証エンドポイントの提供時期・接続方式 | FEG |
| 8 | Azure SQL Database のサービスレベル | テスト／本番のサイジング（DTU / vCore） | JAFFIC 基盤 |

---

## 付録 A. 開発ローカル構築 クイックリファレンス

```powershell
# ---------- 1. 前提ソフト ----------
#  Visual Studio 2026（ASP.NET と Web 開発 / Node.js 開発）
#  Node.js 22 LTS
#  SQL Server 2022 Express + SSMS
npm install -g @angular/cli@20
dotnet tool install --global dotnet-ef

# ---------- 2. リポジトリ ----------
mkdir D:\Work\linye; cd D:\Work\linye
git clone <backend-repo-url>  linyebackend
git clone <frontend-repo-url> linyefrontend

# ---------- 3. DB ----------
# SSMS で GovSystemDB を作成（Japanese_CI_AS）+ ログイン作成
cd linyebackend\src\Api
dotnet ef database update --project ..\Infrastructure --startup-project .
# seed スクリプトを SSMS で実行

# ---------- 4. バックエンド ----------
# appsettings.Development.json を作成（または user-secrets）
cd D:\Work\linye\linyebackend
dotnet restore
dotnet build

# ---------- 5. フロントエンド ----------
cd D:\Work\linye\linyefrontend
npm ci

# ---------- 6. 起動 ----------
# Visual Studio 2026 で Nogyosuisan.sln を開く
#   → スタートアッププロジェクト = Api
#   → 起動プロファイル = "Api (SpaProxy)"
#   → F5
# → http://localhost:4200 が自動で開く
```

---

*本書 版数 1.00 / 作成日 2026-08-06*
